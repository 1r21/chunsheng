{
  "name": "chunsheng",
  "version": "0.0.1",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "docker": "yarn build && docker build -t web:taishan ."
  },
  "dependencies": {
    "axios": "^0.21.0",
    "normalize.css": "^8.0.1",
    "vue": "^3.0.2"
  },
  "devDependencies": {
    "@vue/compiler-sfc": "^3.0.2",
    "vite": "^1.0.0-rc.8"
  }
}
